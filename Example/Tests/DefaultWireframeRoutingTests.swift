//
//  DefaultRouterRoutingTests.swift
//  VISPER-Wireframe_Tests
//
//  Created by bartel on 21.11.17.
//  Copyright © 2017 CocoaPods. All rights reserved.
//

import XCTest
@testable import VISPER_Wireframe
/*
class DefaultWireframeRoutingTests: XCTestCase {
    
    func testCallsRouterForUrl() throws {
        
        let router = MockRouter()
        let wireframe = DefaultWireframe(router: router)
        
        let url = URL(string: "/some/url")!
        
        try wireframe.route(url: url, option: nil, parameters: [:], completion: {})
        XCTAssert(router.invokedRouteCount == 1)
        
        try wireframe.route(url: url, option: MockRoutingOption(), parameters: [:], completion: {})
        XCTAssert(router.invokedRouteCount == 2)
        
    }
    
    func testCallsRoutingOptionProvider() throws {
        
    }
    
    
}
*/
